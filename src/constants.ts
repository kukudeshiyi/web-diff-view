export enum VIEW_MODE {
  SIDE = 'SIDE',
  LINE = 'LINE',
}

export enum TEXT_TYPE {
  NORMAL = 'NORMAL',
  ADD = 'ADD',
  REMOVE = 'REMOVE',
}

export const EOL = '\n';

export const COLLAPSE_LENGTH = 10;
